import styled from 'styled-components';
import { CSS } from '../../variables';

export const Nav = styled.div`
  height: 75px;
  margin-bottom: 80px;
  border-bottom: 1px solid ${CSS['grey-2']};
`

export const Title = styled.h1`

`
